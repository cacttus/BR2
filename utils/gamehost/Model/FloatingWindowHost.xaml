<Controls:MetroWindow x:Class="GameHost.FloatingWindowHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:GameHost"
             mc:Ignorable="d" Width="278.496" Height="294.597" SourceInitialized="MetroWindow_SourceInitialized" ShowMinButton="False" ShowMaxRestoreButton="False" >
    <Controls:MetroWindow.TitleTemplate>
        <DataTemplate>
            <DockPanel Background="{StaticResource IW_TitlebarBrush}">
                <TextBlock Text="{TemplateBinding Content}"
                   TextTrimming="CharacterEllipsis"
                   VerticalAlignment="Center"
                   Margin="8 4 8 4"
                   FontWeight="Normal"
                   FontSize="20"
                   Foreground="{StaticResource IW_TitlebarForegroundBrush}"
                   Background="{StaticResource IW_TitlebarBrush}"
                   FontFamily="Roboto" />
            </DockPanel>
        </DataTemplate>
    </Controls:MetroWindow.TitleTemplate>
    <Grid x:Name="HostGrid">

    </Grid>
</Controls:MetroWindow>
